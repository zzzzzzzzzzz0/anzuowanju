package org.zhscript.clas;

import android.app.Activity;
import android.content.Intent;
import android.net.Uri;
import android.provider.Settings;

/**
 * Created by zzzzzzzzzzz on 17-12-8.
 */

/*
Intent intent = new Intent(Intent.ACTION_MAIN);
intent.setComponent(new ComponentName("com.package.address","com.package.address.MainActivity"));

android.media.action.IMAGE_CAPTURE

*/
public class Test_ {
    public static String[] test__(String[] a, I_ i) {
        //Settings.ACTION_APPLICATION_DETAILS_SETTINGS;
        //Intent.ACTION_SEND_MULTIPLE
        //Intent.EXTRA_TEXT
        //Intent.EXTRA_STREAM
        /*Uri uri = Uri.fromParts("package", i.activity__().getPackageName(), null);
        i.sendMessage__(uri);*/

        //System.out.println(Settings.ACTION_MEMORY_CARD_SETTINGS);
        return null;
    }
}
